import type { RequiredReactRouterConfig } from './types.js';
interface Options {
    strict?: boolean;
    outputDirPath: string;
}
export declare const DEFAULT_OUTPUT_DIR_PATH = "./node_modules";
export declare const TYPE_FILE_NAME = "safe-routes.d.ts";
export declare function build(root: string, config: RequiredReactRouterConfig, options: Options): Promise<void>;
export {};
