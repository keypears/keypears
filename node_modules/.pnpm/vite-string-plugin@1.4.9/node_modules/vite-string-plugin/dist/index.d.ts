import { Plugin } from "vite";

//#region index.d.ts
type ViteStringPluginOpts = {
  /** regex to match on the file path. Default: `/\.(svg|md|xml|txt)$/i` */
  match?: RegExp;
};
/** Vite plugin to import files as string */
declare const stringPlugin: (opts?: ViteStringPluginOpts) => Plugin;
//#endregion
export { ViteStringPluginOpts, stringPlugin };