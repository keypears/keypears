{
  "name": "@keypears/pow5",
  "version": "1.0.0",
  "description": "KeyPears Proof-of-Work (PoW) Algorithm",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "scripts": {
    "clean": "rimraf dist",
    "test": "vitest run",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "lint:check": "eslint . --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write .",
    "sync:from-rust": "cp -r ../pow5-rs/build/bundler/* src/rs-keypears_pow5-bundler/",
    "build": "pnpm run clean && pnpm run build:wasm && pnpm run build:typescript && pnpm run build:cp-assets",
    "build:cp-assets": "pnpm run build:cp-wgsl && pnpm run build:cp-wasm",
    "build:cp-wgsl": "mkdir -p dist && cp src/*.wgsl dist/",
    "build:cp-wasm": "mkdir -p dist/rs-keypears_pow5-inline-base64 && cp src/rs-keypears_pow5-inline-base64/* dist/rs-keypears_pow5-inline-base64/",
    "build:bundler-to-inline-base64": "cp -r src/rs-keypears_pow5-bundler/* src/rs-keypears_pow5-inline-base64/",
    "build:inline-wasm": "tsx build-inline-wasm.ts",
    "build:wasm": "pnpm run build:bundler-to-inline-base64 && pnpm run build:inline-wasm",
    "build:typescript": "tsc -p tsconfig.build.json",
    "prepublishOnly": "pnpm run clean && pnpm run build"
  },
  "engines": {
    "node": ">=20.8.0"
  },
  "packageManager": "pnpm@10.25.0",
  "keywords": [
    "pow"
  ],
  "author": "",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/keypears/keypears.git",
    "directory": "packages/pow5-ts"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "@playwright/browser-chromium": "^1.60.0",
    "@playwright/test": "^1.60.0",
    "@types/node": "^25.6.0",
    "@typescript-eslint/eslint-plugin": "^8.59.0",
    "@typescript-eslint/parser": "^8.59.0",
    "@vitest/browser": "^4.1.5",
    "@vitest/browser-playwright": "^4.1.5",
    "@webgpu/types": "^0.1.69",
    "buffer": "^6.0.3",
    "eslint": "^9.39.4",
    "eslint-import-resolver-typescript": "^4.4.4",
    "eslint-plugin-import": "^2.32.0",
    "eslint-plugin-n": "^17.24.0",
    "eslint-plugin-promise": "^7.2.1",
    "playwright": "^1.60.0",
    "rimraf": "^6.1.3",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vite": "^7.3.2",
    "vite-string-plugin": "^1.5.0",
    "vitest": "^4.1.5"
  },
  "dependencies": {
    "@webbuf/blake3": "^3.8.0",
    "@webbuf/fixedbuf": "^3.8.0",
    "@webbuf/webbuf": "^3.8.0"
  }
}
